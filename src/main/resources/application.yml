server:
  port: ${EXTERNAL_BANK_PORT}

spring:
  config:
    import: optional:file:../.env[.properties]

management:
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never

  endpoints:
    web:
      exposure:
        include: health,info
